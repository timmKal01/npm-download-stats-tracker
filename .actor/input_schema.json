{
    "title": "NPM Download Stats Tracker Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "packages": {
            "title": "Packages",
            "type": "array",
            "editor": "stringList",
            "description": "npm package names to check, e.g. \"react\", \"@angular/core\". At least one required.",
            "prefill": ["react"],
            "default": ["react"]
        },
        "daysBack": {
            "title": "Period length (days)",
            "type": "integer",
            "editor": "number",
            "description": "Length of the period to measure, and also the length of the prior period it's compared against.",
            "default": 7,
            "minimum": 1,
            "maximum": 365
        }
    },
    "required": ["packages"]
}
